# 冷钱包 (Cold Wallet)

完全离线的加密货币钱包应用。

## 🚀 快速开始

### 开发模式

```bash
# 安装依赖（在项目根目录）
cd ../..
pnpm install

# 启动开发服务器
pnpm dev:cold

# 或直接在当前目录
cd packages/cold-wallet
pnpm dev              # 仅启动 Vite 前端
pnpm tauri dev        # 启动完整的 Tauri 应用
```

### 构建

```bash
pnpm tauri build
```

## 📁 项目结构

```
cold-wallet/
├── src/                    # React 前端代码
│   ├── pages/             # 页面组件
│   │   ├── HomePage.tsx   # 首页（创建/导入）
│   │   ├── CreateWalletPage.tsx  # 创建钱包
│   │   ├── ImportWalletPage.tsx  # 导入钱包
│   │   ├── UnlockPage.tsx        # 解锁页面
│   │   ├── WalletPage.tsx        # 钱包主页
│   │   └── SignTransactionPage.tsx # 签名交易
│   ├── components/        # 组件
│   │   └── QRCodeDisplay.tsx  # 二维码显示
│   ├── stores/           # Zustand 状态管理
│   │   └── useWalletStore.ts
│   ├── hooks/            # React Hooks
│   ├── utils/            # 工具函数
│   └── App.tsx           # 路由配置
├── src-tauri/            # Rust 后端代码
│   ├── src/
│   │   ├── main.rs       # Tauri 入口
│   │   ├── crypto/       # 加密相关
│   │   ├── chains/       # 多链支持
│   │   └── qrcode.rs     # 二维码生成
│   └── Cargo.toml        # Rust 依赖
└── package.json
```

## 🔒 安全特性

- ✅ **完全离线** - CSP 禁用所有网络连接
- ✅ **系统级加密存储** - 使用 Keychain/Keystore
- ✅ **内存即时擦除** - 签名后立即清除敏感数据
- ✅ **密码保护** - AES-256-GCM + Argon2id

## 📱 功能

- ✅ 助记词生成/导入
- ✅ 地址生成（ETH, BTC, SOL, BNB, TRON）
- ✅ 交易签名（离线）
- ✅ 二维码生成（地址、签名交易）

## 🔧 技术栈

- **前端**: React 18 + TypeScript + Ant Design Mobile + Zustand
- **后端**: Rust + Tauri 2.0
- **加密**: AES-256-GCM + Argon2id
- **存储**: Keychain/Keystore

## ⚠️ 注意事项

1. **首次编译**：Rust 代码首次编译可能需要 5-10 分钟
2. **网络限制**：冷钱包完全离线，无法访问网络
3. **私钥安全**：助记词和私钥仅在内存中临时存储，使用后立即清除
